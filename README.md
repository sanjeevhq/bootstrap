# bootstrap

One-liner that turns a fresh Mac or Arch box into a copy of my workspace:

```sh
curl -fsSL https://raw.githubusercontent.com/SanjeevThapaUG/bootstrap/main/bootstrap.sh | sh
```

Source of truth is `shared/machines/bootstrap.sh` in the (private) workspace repo; `just publish-bootstrap` mirrors it here.
